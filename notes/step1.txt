Step 1: Created local project folders and website files.

Step 2 – Connected project to GitHub
- Initialized Git repository.
- Linked it to GitHub (origin).
- Pushed initial version online.
- Verified files appear in GitHub.

Step 3 – Automated Resource Creation with Bash
- Created deploy.sh Bash script.
- Added variables and commands to create Azure Resource Group.
- Learned how to make scripts executable and run them.

Step 4 – Created Storage Account and Enabled Static Website Hosting
- Added Azure CLI commands to deploy.sh for storage account creation.
- Used StorageV2 with Standard_LRS.
- Enabled static website feature.
- Confirmed successful output and website endpoint.

Step 5 – Allowed SSH Access and Connected to Capstone VM
- Verified Network Interface and confirmed association with the correct NSG.
- Added inbound NSG rule to allow SSH (port 22) traffic to the VM.
- Queried the public IP of the VM using az vm show --show-details.
- Faced timeout and NIC not found errors during connection attempts.
Fixed issue by verifying all resources were in the same region and reapplying the SSH rule.

Successfully connected to the VM using ssh azureuser@<public-ip>.

Step 4B – Uploaded Website Files to Azure
- Added commands to deploy.sh to upload website files.
- Used az storage blob upload-batch to transfer files.
- Confirmed successful upload and verified live website link.

Step 4C – Pushed Automation Script to GitHub
- Added deploy-infra.sh script to Git version control.
- Committed changes with a descriptive message.
- Fixed push rejection using 'git pull origin main --rebase'.
- Successfully pushed updates to GitHub repository.

Step 5 – Created Virtual Network, Subnet, and VM
- Added VNet, Subnet, and VM creation commands to deploy-infra.sh.
- Used Ubuntu image for VM setup.
- Script now automates full network and VM deployment.
- Verified successful creation and noted public IP address.

Step 6 – Installed and Configured Nginx Web Server
- SSHed into the Capstone VM successfully using the public IP.
- Installed Nginx using 'sudo apt install nginx -y'.
- Started and enabled the Nginx service to run on boot.
- Verified Nginx was active and running.
- Faced package configuration prompt during installation and selected <Ok> to continue.

Step 7 – Tested Nginx Web Server
- Retrieved the public IP address of the Capstone VM using `curl -4 ifconfig.me`.
- Opened the public IP in a web browser to test the Nginx server.
- Initially encountered a “site can’t be reached” error due to HTTP port 80 being blocked.
- Added an inbound NSG rule to allow HTTP (port 80) traffic using Azure CLI.
- Retested and confirmed successful access to the Nginx default welcome page.
- Verified that the web server is live and publicly accessible.

Step 6 – Enabled SSH Access and Deployed Website Files to VM
- Created SSH inbound rule to allow port 22 access.
- Successfully connected to VM using SSH (ssh azureuser@<public-ip>).
- Installed NGINX and verified it running with systemctl status nginx.
- Encountered connection timeout and permission denied issues during setup.
- Resolved by adjusting NSG rule and using sudo for copying website files.
- Uploaded website folder via scp to VM and copied into /var/www/html.
- Verified deployment by visiting VM public IP in browser — site loaded successfully.

Step 8 – Infrastructure Automation Script (infra.sh)
- Created a bash script (infra.sh) to automate the creation of all Azure resources.
- Script includes resource group, virtual network, subnet, NSG, public IP, NIC, and VM.
- Ran the script successfully using 'bash infra.sh' and verified each resource creation.
- Confirmed output showing the public IP address of the VM.
- This ensures repeatable and automated infrastructure setup for the Capstone project.





